# Pollyana's Dolce ERP - reglas para IA

Antes de tocar este repo, lee y sigue:

1. `AGENTS.md`
2. `docs/OPERACION_GIT_AGENTES.md`

No empieces una tarea nueva con archivos sin confirmar. Ejecuta primero:

```bash
git status --short --branch
git log --oneline --decorate -5
```

Si hay cambios pendientes, clasificalos y reportalos antes de modificar.
